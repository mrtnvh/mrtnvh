---
const { title, titleTag = 'h1', showTitle } = Astro.props;
const Title = titleTag;
---

<div class="container">
  <Title class="display-1" class:list={{ 'sr-only': !showTitle }}>{title}</Title>
  <slot />
</div>
